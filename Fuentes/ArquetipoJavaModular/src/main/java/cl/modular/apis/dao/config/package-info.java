/**
 * @author Camilo Contreras
 * 
 *         Esta clase permite configurar las conexiones utilizadas en este
 *         servicio, cada vez que es llamada desde la implementacion de los daos
 *         se genera una conexion distinta por cada hebra hacia el datasource
 *         deseado
 */
package cl.modular.apis.dao.config;
